#include <stdio.h>

int main() {
    // Khai báo và kh?i t?o hai bi?n s? nguyên
    int num1 = 15; // Bi?n num1 lıu giá tr? s? nguyên 15
    int num2 = 25; // Bi?n num2 lıu giá tr? s? nguyên 25

    // Tính t?ng hai s? và lıu k?t qu? vào bi?n khác
    int sum = num1 + num2; // Bi?n sum lıu t?ng c?a num1 và num2

    // In k?t qu? ra màn h?nh
    printf("T?ng c?a %d và %d là: %d\n", num1, num2, sum);

    return 0;
}
